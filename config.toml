[server]
port = 8300
host = "127.0.0.1"
data_dir = "./data"

# Add agents here. Each gets a status pill, @mention routing, and color.
# "cwd" is the working directory for the agent's terminal session.
# "command" is the CLI command (must be on PATH).

[agents.meera]
command = "codex"
cwd = "C:/Users/shiva/Documents/NinjaTrader 8/bin/Custom/Strategies/.agent/meera"
color = "#10a37f"
label = "Meera"

[agents.ishika]
command = "claude"
cwd = "C:/Users/shiva/Documents/NinjaTrader 8/bin/Custom/Strategies/.agent/ishika"
color = "#da7756"
label = "Ishika"

[agents.rashmika]
command = "gemini"
cwd = "C:/Users/shiva/Documents/NinjaTrader 8/bin/Custom/Strategies/.agent/rashmika"
color = "#4285f4"
label = "Rashmika"

[routing]
# "none" = only route on explicit @mention. "all" = route to all agents by default.
default = "none"
max_agent_hops = 4

[mcp]
http_port = 8200
sse_port = 8201

[presence]
timeout_seconds = 600
heartbeat_seconds = 30

[images]
upload_dir = "./uploads"
max_size_mb = 10
